# Нормализация переводов строк: текст хранится с LF в репозитории
* text=auto eol=lf

# Windows-скрипты должны оставаться с CRLF
*.bat text eol=crlf

# Бинарные файлы — без преобразований
*.xlsx binary
*.docx binary
*.pdf binary
*.db binary
